# MCP Gateway Server専用のDockerfile
FROM oven/bun:1-alpine

WORKDIR /app

# 依存関係のインストール
COPY package.json ./
RUN bun install --production

# ソースコードをコピー
COPY tsconfig.node.json ./
COPY server ./server

# MCPサーバーをstdioモードで起動
CMD ["bun", "server/index.ts"]