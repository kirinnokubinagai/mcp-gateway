FROM oven/bun:latest

WORKDIR /app

# 依存関係のインストール
COPY package.json ./
RUN bun install

# ソースコードをコピー
COPY . .

# ビルド
RUN bun run build

EXPOSE 3002

# Viteのプレビューサーバーを使用（環境変数でポートを指定）
CMD ["sh", "-c", "bun run preview -- --host 0.0.0.0 --port ${CLIENT_PORT:-3002}"]