{
  "name": "mcp-gateway",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "proxy": "cd mcp-proxy-server && bun run server.ts",
    "proxy:watch": "node scripts/watch-config.js",
    "clean": "rm -rf dist node_modules"
  },
  "devDependencies": {
    "@types/bun": "^1.2.17"
  }
}
